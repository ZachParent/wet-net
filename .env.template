# Copy this file to `.env` and fill the values.
# Secrets should not be committed.

# Required: write token for pushing to Hugging Face hub
HF_TOKEN=hf_your_write_token_here

# Required for real-data runs: URL to download the proprietary parquet/zip
# (leave empty or remove when running with --mock)
#WETNET_DATA_URL=https://example.com/anomalous_consumption.parquet

# Optional performance and stability knobs
# Limit training samples to cap GPU memory (e.g., 120000 for full runs, smaller for smoke tests)
# WETNET_MAX_SAMPLES=120000
# Shorten epochs for quick validation; unset for full training
# WETNET_FAST=
# Increase HF Hub timeouts for large uploads
HF_HUB_HTTP_TIMEOUT=3000
# Use Rust-based uploader when available (can speed large pushes)
HF_HUB_ENABLE_HF_TRANSFER=1
